@using ASP.Server.Controllers
@model EditGenreModel
@{
    ViewData["Title"] = "Add a Genre";
}
<style>
.input-group {
    margin-top:10px;
    display:flex;
    justify-content:space-between;
    flex-wrap:wrap;
}
input, textarea, select {
    flex-basis:85%;
}
label {
    flex-basis:10%;
}
</style>

<!-- Voici une facon de faire, mais ce n'est pas la seul, a vous de jouer ! -->
<form asp-controller="Genre" asp-action="Update"> <!-- [B.]On peut mettre la methode post -->
    <div class="input-group">
        <label asp-for="Name"></label>
        <input asp-for="Name" required />
        <input asp-for="Id" type="hidden" />
    </div>
    <input type="submit" value="Update" style="width: 100%; margin-top: 50px" />
</form>